{
  "name": "lumora-daemon",
  "version": "1.0.0",
  "description": "Lumora Daemon — local agent runtime that connects your machine to the Lumora dashboard for remote AI task execution.",
  "type": "module",
  "bin": {
    "lumora-daemon": "dist/bin/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch bin/cli.ts",
    "test": "npm run build && node --test dist/src/**/*.test.js dist/src/core/**/*.test.js"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "execa": "^9.5.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lumora/lumora-daemon"
  },
  "keywords": [
    "lumora",
    "daemon",
    "agent",
    "ai",
    "runtime",
    "claude-code",
    "codex",
    "onchainos"
  ]
}
